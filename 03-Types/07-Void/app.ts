function printValues(a: number, b: number): void {
    console.log(`Values are ${a} and ${b}`);
}
printValues(2, 3);

let Value: void = undefined;
console.log(`Value is ${Value} with type ${typeof Value}`);